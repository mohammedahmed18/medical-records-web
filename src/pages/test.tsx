import { Button } from '@rapiddata-ai/design-system';

import Layout from '@/components/layout';
const TestPage = () => {
  return (
    <Layout>
      <Button>Click me</Button>;
    </Layout>
  );
};

export default TestPage;
